# Datadog Agent Configuration Fix
# Add these settings to your datadog.yaml configuration

# Basic Configuration - ALREADY CONFIGURED
api_key: a5d3029c8cd5837f0d44f1d1fb3ed171
site: datadoghq.eu

# APM Configuration - THIS IS LIKELY MISSING
apm_config:
  enabled: true
  apm_non_local_traffic: true
  receiver_port: 8126

# OTLP Configuration - ALREADY CONFIGURED
otlp_config:
  receiver:
    protocols:
      grpc:
        endpoint: localhost:4317
      http:
        endpoint: localhost:4318
  traces:
    enabled: true
  metrics:
    enabled: true
  # Debug should be disabled in production, but can help troubleshoot
  debug:
    verbosity: normal

# Logging Configuration (optional for troubleshooting)
log_level: debug
